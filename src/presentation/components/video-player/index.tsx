import React from "react"

type VideoPlayerProps = {
    source: string;
}

export const VideoPlayer = ( { source }: VideoPlayerProps ) => {
    return (
        <>
            VideoPlayer
        </>
    )
}